package jeu.models;

import java.awt.*;

public class Bouton extends Rectangle{
    public Bouton(int x, int y, Color couleur, int largeur, int hauteur) {
        super(x, y, couleur, largeur, hauteur);
    }
}
